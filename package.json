{
  "name": "vinaup-agency-frontend",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:jenahair": "npm run dev --workspace=apps/jenahair",
    "dev:tlt": "npm run dev --workspace=apps/the-local-travel",
    "build:jenahair": "npm run build --workspace=apps/jenahair",
    "start:jenahair": "npm run start --workspace=apps/jenahair",
    "dev:vitinhnet": "npm run dev --workspace=apps/vitinhnet",
    "build:vitinhnet": "npm run build --workspace=apps/vitinhnet",
    "start:vitinhnet": "npm run start --workspace=apps/vitinhnet",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-check-file": "^3.3.1"
  }
}
